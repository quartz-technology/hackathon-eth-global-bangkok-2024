{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "codegen": "graph codegen",
    "create-local": "graph create --node http://localhost:8020/ auto-rebalencer",
    "remove-local": "graph remove --node http://localhost:8020/ auto-rebalencer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 auto-rebalencer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "0.34.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}